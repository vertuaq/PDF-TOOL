<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>PDF Free Editor RSA</title>
  <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;1,300&display=swap" rel="stylesheet"/>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/pdf-lib/1.17.1/pdf-lib.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/mammoth/1.6.0/mammoth.browser.min.js"></script>
  <style>
    :root {
      --bg: #0a0a0f;
      --surface: #13131c;
      --card: #1a1a27;
      --border: #2a2a3d;
      --accent: #00e5a0;
      --accent2: #7b5ea7;
      --text: #e8e8f0;
      --muted: #7a7a99;
      --danger: #ff4f6a;
      --white: #ffffff;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: 'DM Sans', sans-serif;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Background grid */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(0,229,160,0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,229,160,0.04) 1px, transparent 1px);
      background-size: 40px 40px;
      pointer-events: none;
      z-index: 0;
    }

    /* Glows */
    body::after {
      content: '';
      position: fixed;
      top: -200px;
      left: -200px;
      width: 600px;
      height: 600px;
      background: radial-gradient(circle, rgba(0,229,160,0.08) 0%, transparent 70%);
      pointer-events: none;
      z-index: 0;
    }

    /* NAV */
    nav {
      position: sticky;
      top: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1.1rem 2.5rem;
      background: rgba(10,10,15,0.85);
      backdrop-filter: blur(16px);
      border-bottom: 1px solid var(--border);
    }

    .logo {
      font-family: 'Syne', sans-serif;
      font-weight: 800;
      font-size: 1.35rem;
      letter-spacing: -0.5px;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .logo span.green { color: var(--accent); }
    .logo span.badge {
      background: var(--accent);
      color: #0a0a0f;
      font-size: 0.6rem;
      font-weight: 800;
      padding: 2px 7px;
      border-radius: 99px;
      letter-spacing: 1px;
      text-transform: uppercase;
    }

    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }

    .nav-links a {
      color: var(--muted);
      text-decoration: none;
      font-size: 0.9rem;
      transition: color 0.2s;
    }

    .nav-links a:hover { color: var(--accent); }

    /* HERO */
    .hero {
      position: relative;
      z-index: 1;
      text-align: center;
      padding: 6rem 2rem 4rem;
    }

    .hero-eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      background: rgba(0,229,160,0.08);
      border: 1px solid rgba(0,229,160,0.2);
      color: var(--accent);
      font-size: 0.8rem;
      font-weight: 500;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      padding: 0.4rem 1rem;
      border-radius: 99px;
      margin-bottom: 1.8rem;
      animation: fadeDown 0.6s ease both;
    }

    h1 {
      font-family: 'Syne', sans-serif;
      font-size: clamp(2.8rem, 7vw, 5.5rem);
      font-weight: 800;
      line-height: 1.05;
      letter-spacing: -2px;
      margin-bottom: 1.5rem;
      animation: fadeDown 0.6s 0.1s ease both;
    }

    h1 .accent-line {
      background: linear-gradient(90deg, var(--accent), #00c9ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero-sub {
      color: var(--muted);
      font-size: 1.15rem;
      max-width: 560px;
      margin: 0 auto 2.5rem;
      line-height: 1.7;
      animation: fadeDown 0.6s 0.2s ease both;
    }

    .hero-flag {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      font-size: 0.85rem;
      color: var(--muted);
      animation: fadeDown 0.6s 0.3s ease both;
    }

    /* TOOLS GRID */
    .section {
      position: relative;
      z-index: 1;
      max-width: 1200px;
      margin: 0 auto;
      padding: 3rem 2rem 5rem;
    }

    .section-title {
      font-family: 'Syne', sans-serif;
      font-size: 1.05rem;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--muted);
      margin-bottom: 2rem;
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .section-title::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--border);
    }

    .tools-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 1.2rem;
    }

    .tool-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 1.8rem;
      cursor: pointer;
      transition: all 0.25s ease;
      position: relative;
      overflow: hidden;
      animation: fadeUp 0.5s ease both;
    }

    .tool-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, rgba(0,229,160,0.06), transparent);
      opacity: 0;
      transition: opacity 0.25s;
    }

    .tool-card:hover {
      border-color: var(--accent);
      transform: translateY(-3px);
      box-shadow: 0 12px 40px rgba(0,229,160,0.12);
    }

    .tool-card:hover::before { opacity: 1; }

    .tool-icon {
      width: 48px;
      height: 48px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.4rem;
      margin-bottom: 1rem;
    }

    .tool-icon.green { background: rgba(0,229,160,0.12); }
    .tool-icon.purple { background: rgba(123,94,167,0.18); }
    .tool-icon.blue { background: rgba(0,180,255,0.12); }
    .tool-icon.orange { background: rgba(255,150,50,0.12); }
    .tool-icon.red { background: rgba(255,79,106,0.12); }

    .tool-card h3 {
      font-family: 'Syne', sans-serif;
      font-size: 1rem;
      font-weight: 700;
      margin-bottom: 0.4rem;
    }

    .tool-card p {
      font-size: 0.85rem;
      color: var(--muted);
      line-height: 1.6;
    }

    .tool-tag {
      display: inline-block;
      background: rgba(0,229,160,0.08);
      color: var(--accent);
      font-size: 0.7rem;
      font-weight: 600;
      letter-spacing: 0.5px;
      padding: 2px 8px;
      border-radius: 99px;
      margin-top: 1rem;
      text-transform: uppercase;
    }

    /* MODAL */
    .modal-overlay {
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(5,5,10,0.85);
      backdrop-filter: blur(8px);
      z-index: 200;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .modal-overlay.open { display: flex; }

    .modal {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 2.2rem;
      width: 100%;
      max-width: 540px;
      max-height: 90vh;
      overflow-y: auto;
      position: relative;
      animation: scaleIn 0.2s ease;
    }

    .modal-close {
      position: absolute;
      top: 1.2rem;
      right: 1.2rem;
      background: var(--border);
      border: none;
      color: var(--muted);
      width: 32px;
      height: 32px;
      border-radius: 50%;
      cursor: pointer;
      font-size: 1rem;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s;
    }

    .modal-close:hover { background: var(--danger); color: white; }

    .modal h2 {
      font-family: 'Syne', sans-serif;
      font-size: 1.4rem;
      font-weight: 800;
      margin-bottom: 0.4rem;
    }

    .modal-desc {
      color: var(--muted);
      font-size: 0.9rem;
      margin-bottom: 1.8rem;
      line-height: 1.6;
    }

    /* Drop zone */
    .drop-zone {
      border: 2px dashed var(--border);
      border-radius: 12px;
      padding: 2.5rem 1.5rem;
      text-align: center;
      cursor: pointer;
      transition: all 0.2s;
      margin-bottom: 1.2rem;
    }

    .drop-zone:hover, .drop-zone.drag-over {
      border-color: var(--accent);
      background: rgba(0,229,160,0.04);
    }

    .drop-zone .drop-icon { font-size: 2.5rem; margin-bottom: 0.8rem; }
    .drop-zone p { color: var(--muted); font-size: 0.9rem; }
    .drop-zone strong { color: var(--accent); }

    input[type="file"] { display: none; }

    /* File list */
    .file-list {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      margin-bottom: 1.2rem;
    }

    .file-item {
      display: flex;
      align-items: center;
      gap: 0.8rem;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 0.7rem 1rem;
      font-size: 0.85rem;
    }

    .file-item .file-name { flex: 1; color: var(--text); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .file-item .file-size { color: var(--muted); font-size: 0.78rem; }
    .file-item .remove-btn { cursor: pointer; color: var(--danger); background: none; border: none; font-size: 1rem; }

    /* Buttons */
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      padding: 0.75rem 1.6rem;
      border-radius: 10px;
      font-family: 'DM Sans', sans-serif;
      font-size: 0.95rem;
      font-weight: 500;
      cursor: pointer;
      border: none;
      transition: all 0.2s;
      width: 100%;
    }

    .btn-primary {
      background: var(--accent);
      color: #0a0a0f;
      font-weight: 700;
    }

    .btn-primary:hover { background: #00ffb3; box-shadow: 0 0 20px rgba(0,229,160,0.3); }
    .btn-primary:disabled { opacity: 0.5; cursor: not-allowed; }

    .btn-secondary {
      background: var(--surface);
      color: var(--text);
      border: 1px solid var(--border);
    }

    .btn-secondary:hover { border-color: var(--accent); color: var(--accent); }

    /* Progress */
    .progress-bar {
      width: 100%;
      height: 4px;
      background: var(--border);
      border-radius: 99px;
      overflow: hidden;
      margin: 1rem 0;
      display: none;
    }

    .progress-bar.visible { display: block; }

    .progress-fill {
      height: 100%;
      background: linear-gradient(90deg, var(--accent), #00c9ff);
      border-radius: 99px;
      transition: width 0.3s ease;
      width: 0%;
    }

    /* Status message */
    .status-msg {
      font-size: 0.85rem;
      padding: 0.8rem 1rem;
      border-radius: 8px;
      margin-top: 1rem;
      display: none;
    }

    .status-msg.success {
      display: block;
      background: rgba(0,229,160,0.1);
      border: 1px solid rgba(0,229,160,0.3);
      color: var(--accent);
    }

    .status-msg.error {
      display: block;
      background: rgba(255,79,106,0.1);
      border: 1px solid rgba(255,79,106,0.3);
      color: var(--danger);
    }

    /* FOOTER */
    footer {
      position: relative;
      z-index: 1;
      border-top: 1px solid var(--border);
      padding: 2rem 2.5rem;
      text-align: center;
      color: var(--muted);
      font-size: 0.85rem;
    }

    footer span { color: var(--accent); }

    /* ANIMATIONS */
    @keyframes fadeDown {
      from { opacity: 0; transform: translateY(-12px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(16px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes scaleIn {
      from { opacity: 0; transform: scale(0.95); }
      to { opacity: 1; transform: scale(1); }
    }

    .tool-card:nth-child(1) { animation-delay: 0.05s; }
    .tool-card:nth-child(2) { animation-delay: 0.10s; }
    .tool-card:nth-child(3) { animation-delay: 0.15s; }
    .tool-card:nth-child(4) { animation-delay: 0.20s; }
    .tool-card:nth-child(5) { animation-delay: 0.25s; }
    .tool-card:nth-child(6) { animation-delay: 0.30s; }

    /* Responsive */
    @media (max-width: 600px) {
      nav { padding: 1rem 1.2rem; }
      .nav-links { display: none; }
      h1 { font-size: 2.5rem; }
      .section { padding: 2rem 1rem 3rem; }
    }

    /* Range input */
    input[type="range"] {
      width: 100%;
      accent-color: var(--accent);
      margin: 0.5rem 0;
    }

    label.field-label {
      display: block;
      font-size: 0.82rem;
      color: var(--muted);
      margin-bottom: 0.4rem;
      font-weight: 500;
      letter-spacing: 0.5px;
      text-transform: uppercase;
    }

    .field-row {
      margin-bottom: 1.2rem;
    }

    .range-value {
      color: var(--accent);
      font-weight: 700;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="logo">
    📄 PDF<span class="green">Free</span> <span class="badge">RSA</span>
  </div>
  <ul class="nav-links">
    <li><a href="#tools">Tools</a></li>
    <li><a href="#about">About</a></li>
  </ul>
</nav>

<!-- HERO -->
<div class="hero">
  <div class="hero-eyebrow">✦ 100% Free · No Sign-up Required</div>
  <h1>Edit PDFs.<br><span class="accent-line">Completely Free.</span></h1>
  <p class="hero-sub">A full suite of document tools — merge, split, compress, convert and more. No subscriptions, no watermarks, no excuses.</p>
  <div class="hero-flag">🇿🇦 Made for South Africa, free for the world</div>
</div>

<!-- TOOLS -->
<section class="section" id="tools">
  <div class="section-title">All Tools</div>
  <div class="tools-grid">

    <div class="tool-card" onclick="openModal('merge')">
      <div class="tool-icon green">🔗</div>
      <h3>Merge PDFs</h3>
      <p>Combine multiple PDF files into one document instantly.</p>
      <span class="tool-tag">PDF → PDF</span>
    </div>

    <div class="tool-card" onclick="openModal('split')">
      <div class="tool-icon purple">✂️</div>
      <h3>Split PDF</h3>
      <p>Extract specific pages or split a PDF into individual pages.</p>
      <span class="tool-tag">PDF → PDF</span>
    </div>

    <div class="tool-card" onclick="openModal('compress')">
      <div class="tool-icon blue">🗜️</div>
      <h3>Compress PDF</h3>
      <p>Reduce PDF file size while keeping the quality intact.</p>
      <span class="tool-tag">Optimize</span>
    </div>

    <div class="tool-card" onclick="openModal('word-to-pdf')">
      <div class="tool-icon orange">📝</div>
      <h3>Word to PDF</h3>
      <p>Convert your .docx Word documents to PDF format.</p>
      <span class="tool-tag">DOCX → PDF</span>
    </div>

    <div class="tool-card" onclick="openModal('rotate')">
      <div class="tool-icon green">🔄</div>
      <h3>Rotate PDF</h3>
      <p>Rotate pages in your PDF to the correct orientation.</p>
      <span class="tool-tag">PDF → PDF</span>
    </div>

    <div class="tool-card" onclick="openModal('watermark')">
      <div class="tool-icon red">🔏</div>
      <h3>Watermark PDF</h3>
      <p>Add a custom text watermark to protect your documents.</p>
      <span class="tool-tag">PDF → PDF</span>
    </div>

  </div>
</section>

<!-- ABOUT -->
<section class="section" id="about">
  <div class="section-title">About</div>
  <div style="max-width:600px; color: var(--muted); font-size:0.95rem; line-height:1.8;">
    <p><strong style="color:var(--text)">PDF Free Editor RSA</strong> was built because too many document tools charge you just to do basic things with your own files. We believe every student and professional should have access to the same tools — for free, forever.</p>
    <br/>
    <p>All processing happens <strong style="color:var(--accent)">right in your browser</strong>. Your files are never uploaded to any server. Your data is yours.</p>
  </div>
</section>

<!-- FOOTER -->
<footer>
  Built with ❤️ for <span>South Africa</span> · PDF Free Editor RSA · Completely Free, Forever
</footer>

<!-- ==================== MODALS ==================== -->

<!-- MERGE MODAL -->
<div class="modal-overlay" id="modal-merge">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('merge')">✕</button>
    <h2>🔗 Merge PDFs</h2>
    <p class="modal-desc">Upload two or more PDF files. They'll be merged in the order listed.</p>
    <div class="drop-zone" id="drop-merge" onclick="document.getElementById('input-merge').click()">
      <div class="drop-icon">📂</div>
      <p><strong>Click to select</strong> or drag & drop PDFs here</p>
      <p style="font-size:0.78rem; margin-top:0.4rem;">Multiple files allowed</p>
    </div>
    <input type="file" id="input-merge" multiple accept=".pdf" onchange="handleFiles('merge', this.files)"/>
    <div class="file-list" id="list-merge"></div>
    <div class="progress-bar" id="prog-merge"><div class="progress-fill" id="progfill-merge"></div></div>
    <button class="btn btn-primary" id="btn-merge" onclick="runMerge()" disabled>Merge & Download</button>
    <div class="status-msg" id="status-merge"></div>
  </div>
</div>

<!-- SPLIT MODAL -->
<div class="modal-overlay" id="modal-split">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('split')">✕</button>
    <h2>✂️ Split PDF</h2>
    <p class="modal-desc">Upload a PDF and specify which pages to extract (e.g. 1,3,5-7). Leave blank to split all pages.</p>
    <div class="drop-zone" onclick="document.getElementById('input-split').click()">
      <div class="drop-icon">📂</div>
      <p><strong>Click to select</strong> a PDF file</p>
    </div>
    <input type="file" id="input-split" accept=".pdf" onchange="handleFiles('split', this.files)"/>
    <div class="file-list" id="list-split"></div>
    <div class="field-row">
      <label class="field-label">Pages to extract (e.g. 1,2,5-8) — blank = all</label>
      <input type="text" id="split-pages" placeholder="e.g. 1,3,5-7"
        style="width:100%;background:var(--surface);border:1px solid var(--border);border-radius:8px;padding:0.7rem 1rem;color:var(--text);font-family:inherit;font-size:0.9rem;outline:none;"/>
    </div>
    <div class="progress-bar" id="prog-split"><div class="progress-fill" id="progfill-split"></div></div>
    <button class="btn btn-primary" id="btn-split" onclick="runSplit()" disabled>Split & Download</button>
    <div class="status-msg" id="status-split"></div>
  </div>
</div>

<!-- COMPRESS MODAL -->
<div class="modal-overlay" id="modal-compress">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('compress')">✕</button>
    <h2>🗜️ Compress PDF</h2>
    <p class="modal-desc">Reduce the size of your PDF. Note: Browser-based compression removes metadata and optimizes structure. For image-heavy PDFs, download the result and check quality.</p>
    <div class="drop-zone" onclick="document.getElementById('input-compress').click()">
      <div class="drop-icon">📂</div>
      <p><strong>Click to select</strong> a PDF file</p>
    </div>
    <input type="file" id="input-compress" accept=".pdf" onchange="handleFiles('compress', this.files)"/>
    <div class="file-list" id="list-compress"></div>
    <div class="progress-bar" id="prog-compress"><div class="progress-fill" id="progfill-compress"></div></div>
    <button class="btn btn-primary" id="btn-compress" onclick="runCompress()" disabled>Compress & Download</button>
    <div class="status-msg" id="status-compress"></div>
  </div>
</div>

<!-- WORD TO PDF MODAL -->
<div class="modal-overlay" id="modal-word-to-pdf">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('word-to-pdf')">✕</button>
    <h2>📝 Word to PDF</h2>
    <p class="modal-desc">Upload a .docx file and convert it to a clean PDF. The text content will be preserved in a clean, readable format.</p>
    <div class="drop-zone" onclick="document.getElementById('input-word').click()">
      <div class="drop-icon">📂</div>
      <p><strong>Click to select</strong> a .docx file</p>
    </div>
    <input type="file" id="input-word" accept=".docx" onchange="handleFiles('word-to-pdf', this.files)"/>
    <div class="file-list" id="list-word-to-pdf"></div>
    <div class="progress-bar" id="prog-word-to-pdf"><div class="progress-fill" id="progfill-word-to-pdf"></div></div>
    <button class="btn btn-primary" id="btn-word-to-pdf" onclick="runWordToPDF()" disabled>Convert & Download</button>
    <div class="status-msg" id="status-word-to-pdf"></div>
  </div>
</div>

<!-- ROTATE MODAL -->
<div class="modal-overlay" id="modal-rotate">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('rotate')">✕</button>
    <h2>🔄 Rotate PDF</h2>
    <p class="modal-desc">Upload a PDF and choose the rotation angle.</p>
    <div class="drop-zone" onclick="document.getElementById('input-rotate').click()">
      <div class="drop-icon">📂</div>
      <p><strong>Click to select</strong> a PDF file</p>
    </div>
    <input type="file" id="input-rotate" accept=".pdf" onchange="handleFiles('rotate', this.files)"/>
    <div class="file-list" id="list-rotate"></div>
    <div class="field-row">
      <label class="field-label">Rotation Angle</label>
      <div style="display:flex;gap:0.8rem;flex-wrap:wrap;">
        <button class="btn btn-secondary" style="width:auto;padding:0.5rem 1.2rem;" onclick="setRotation(90)">90° ↻</button>
        <button class="btn btn-secondary" style="width:auto;padding:0.5rem 1.2rem;" onclick="setRotation(180)">180° ↻</button>
        <button class="btn btn-secondary" style="width:auto;padding:0.5rem 1.2rem;" onclick="setRotation(270)">270° ↻</button>
      </div>
      <p style="font-size:0.8rem;color:var(--muted);margin-top:0.5rem;">Selected: <span id="rotate-val" style="color:var(--accent)">90°</span></p>
    </div>
    <div class="progress-bar" id="prog-rotate"><div class="progress-fill" id="progfill-rotate"></div></div>
    <button class="btn btn-primary" id="btn-rotate" onclick="runRotate()" disabled>Rotate & Download</button>
    <div class="status-msg" id="status-rotate"></div>
  </div>
</div>

<!-- WATERMARK MODAL -->
<div class="modal-overlay" id="modal-watermark">
  <div class="modal">
    <button class="modal-close" onclick="closeModal('watermark')">✕</button>
    <h2>🔏 Watermark PDF</h2>
    <p class="modal-desc">Add a diagonal text watermark to every page of your PDF.</p>
    <div class="drop-zone" onclick="document.getElementById('input-watermark').click()">
      <div class="drop-icon">📂</div>
      <p><strong>Click to select</strong> a PDF file</p>
    </div>
    <input type="file" id="input-watermark" accept=".pdf" onchange="handleFiles('watermark', this.files)"/>
    <div class="file-list" id="list-watermark"></div>
    <div class="field-row">
      <label class="field-label">Watermark Text</label>
      <input type="text" id="watermark-text" placeholder="e.g. CONFIDENTIAL"
        style="width:100%;background:var(--surface);border:1px solid var(--border);border-radius:8px;padding:0.7rem 1rem;color:var(--text);font-family:inherit;font-size:0.9rem;outline:none;"/>
    </div>
    <div class="progress-bar" id="prog-watermark"><div class="progress-fill" id="progfill-watermark"></div></div>
    <button class="btn btn-primary" id="btn-watermark" onclick="runWatermark()" disabled>Add Watermark & Download</button>
    <div class="status-msg" id="status-watermark"></div>
  </div>
</div>

<script>
  // ===================== FILE STATE =====================
  const fileStore = {};
  let rotationAngle = 90;

  function setRotation(deg) {
    rotationAngle = deg;
    document.getElementById('rotate-val').textContent = deg + '°';
  }

  // ===================== MODAL =====================
  function openModal(id) {
    document.getElementById('modal-' + id).classList.add('open');
    document.body.style.overflow = 'hidden';
  }

  function closeModal(id) {
    document.getElementById('modal-' + id).classList.remove('open');
    document.body.style.overflow = '';
  }

  document.querySelectorAll('.modal-overlay').forEach(overlay => {
    overlay.addEventListener('click', (e) => {
      if (e.target === overlay) {
        overlay.classList.remove('open');
        document.body.style.overflow = '';
      }
    });
  });

  // ===================== DRAG & DROP =====================
  document.querySelectorAll('.drop-zone').forEach(zone => {
    zone.addEventListener('dragover', e => { e.preventDefault(); zone.classList.add('drag-over'); });
    zone.addEventListener('dragleave', () => zone.classList.remove('drag-over'));
    zone.addEventListener('drop', e => {
      e.preventDefault();
      zone.classList.remove('drag-over');
      const inputId = zone.getAttribute('onclick');
    });
  });

  // ===================== FILE HANDLING =====================
  function handleFiles(tool, files) {
    if (!files || files.length === 0) return;
    if (!fileStore[tool]) fileStore[tool] = [];
    Array.from(files).forEach(f => fileStore[tool].push(f));
    renderFileList(tool);
    document.getElementById('btn-' + tool).disabled = false;
    clearStatus(tool);
  }

  function renderFileList(tool) {
    const list = document.getElementById('list-' + tool);
    list.innerHTML = '';
    (fileStore[tool] || []).forEach((f, i) => {
      const item = document.createElement('div');
      item.className = 'file-item';
      item.innerHTML = `<span>📄</span><span class="file-name">${f.name}</span><span class="file-size">${formatSize(f.size)}</span><button class="remove-btn" onclick="removeFile('${tool}',${i})">✕</button>`;
      list.appendChild(item);
    });
  }

  function removeFile(tool, idx) {
    fileStore[tool].splice(idx, 1);
    renderFileList(tool);
    if (!fileStore[tool] || fileStore[tool].length === 0) {
      document.getElementById('btn-' + tool).disabled = true;
    }
  }

  function formatSize(bytes) {
    if (bytes < 1024) return bytes + ' B';
    if (bytes < 1024 * 1024) return (bytes / 1024).toFixed(1) + ' KB';
    return (bytes / (1024 * 1024)).toFixed(1) + ' MB';
  }

  // ===================== PROGRESS =====================
  function showProgress(tool, pct) {
    document.getElementById('prog-' + tool).classList.add('visible');
    document.getElementById('progfill-' + tool).style.width = pct + '%';
  }

  function hideProgress(tool) {
    setTimeout(() => document.getElementById('prog-' + tool).classList.remove('visible'), 600);
  }

  function showStatus(tool, msg, type) {
    const el = document.getElementById('status-' + tool);
    el.textContent = msg;
    el.className = 'status-msg ' + type;
  }

  function clearStatus(tool) {
    const el = document.getElementById('status-' + tool);
    el.className = 'status-msg';
  }

  function download(bytes, filename) {
    const blob = new Blob([bytes], { type: 'application/pdf' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    a.click();
    URL.revokeObjectURL(url);
  }

  // ===================== MERGE =====================
  async function runMerge() {
    const files = fileStore['merge'];
    if (!files || files.length < 2) { showStatus('merge', '⚠️ Please add at least 2 PDF files.', 'error'); return; }
    try {
      showProgress('merge', 10);
      const { PDFDocument } = PDFLib;
      const merged = await PDFDocument.create();
      for (let i = 0; i < files.length; i++) {
        showProgress('merge', 10 + (i / files.length) * 80);
        const ab = await files[i].arrayBuffer();
        const doc = await PDFDocument.load(ab);
        const pages = await merged.copyPages(doc, doc.getPageIndices());
        pages.forEach(p => merged.addPage(p));
      }
      showProgress('merge', 95);
      const bytes = await merged.save();
      showProgress('merge', 100);
      hideProgress('merge');
      download(bytes, 'merged.pdf');
      showStatus('merge', '✅ PDF merged successfully!', 'success');
    } catch(e) {
      hideProgress('merge');
      showStatus('merge', '❌ Error: ' + e.message, 'error');
    }
  }

  // ===================== SPLIT =====================
  function parsePageRanges(str, total) {
    if (!str.trim()) return Array.from({length: total}, (_, i) => i);
    const pages = new Set();
    str.split(',').forEach(part => {
      part = part.trim();
      if (part.includes('-')) {
        const [a, b] = part.split('-').map(n => parseInt(n) - 1);
        for (let i = a; i <= Math.min(b, total - 1); i++) pages.add(i);
      } else {
        const n = parseInt(part) - 1;
        if (n >= 0 && n < total) pages.add(n);
      }
    });
    return [...pages].sort((a, b) => a - b);
  }

  async function runSplit() {
    const files = fileStore['split'];
    if (!files || files.length === 0) { showStatus('split', '⚠️ Please select a PDF file.', 'error'); return; }
    try {
      showProgress('split', 20);
      const { PDFDocument } = PDFLib;
      const ab = await files[0].arrayBuffer();
      const srcDoc = await PDFDocument.load(ab);
      const total = srcDoc.getPageCount();
      const pageStr = document.getElementById('split-pages').value;
      const indices = parsePageRanges(pageStr, total);
      showProgress('split', 60);
      const newDoc = await PDFDocument.create();
      const copied = await newDoc.copyPages(srcDoc, indices);
      copied.forEach(p => newDoc.addPage(p));
      showProgress('split', 90);
      const bytes = await newDoc.save();
      showProgress('split', 100);
      hideProgress('split');
      download(bytes, 'split.pdf');
      showStatus('split', `✅ Extracted ${indices.length} page(s) successfully!`, 'success');
    } catch(e) {
      hideProgress('split');
      showStatus('split', '❌ Error: ' + e.message, 'error');
    }
  }

  // ===================== COMPRESS =====================
  async function runCompress() {
    const files = fileStore['compress'];
    if (!files || files.length === 0) { showStatus('compress', '⚠️ Please select a PDF file.', 'error'); return; }
    try {
      showProgress('compress', 20);
      const { PDFDocument } = PDFLib;
      const ab = await files[0].arrayBuffer();
      const doc = await PDFDocument.load(ab, { updateMetadata: false });
      showProgress('compress', 60);
      const bytes = await doc.save({ useObjectStreams: true });
      showProgress('compress', 100);
      hideProgress('compress');
      const orig = files[0].size;
      const newSize = bytes.byteLength;
      const saved = ((orig - newSize) / orig * 100).toFixed(1);
      download(bytes, 'compressed.pdf');
      showStatus('compress', `✅ Done! ${formatSize(orig)} → ${formatSize(newSize)} (${saved > 0 ? saved + '% smaller' : 'optimized'})`, 'success');
    } catch(e) {
      hideProgress('compress');
      showStatus('compress', '❌ Error: ' + e.message, 'error');
    }
  }

  // ===================== WORD TO PDF =====================
  async function runWordToPDF() {
    const files = fileStore['word-to-pdf'];
    if (!files || files.length === 0) { showStatus('word-to-pdf', '⚠️ Please select a .docx file.', 'error'); return; }
    try {
      showProgress('word-to-pdf', 20);
      const ab = await files[0].arrayBuffer();
      const result = await mammoth.extractRawText({ arrayBuffer: ab });
      const text = result.value;
      showProgress('word-to-pdf', 60);

      const { PDFDocument, StandardFonts, rgb } = PDFLib;
      const pdfDoc = await PDFDocument.create();
      const font = await pdfDoc.embedFont(StandardFonts.Helvetica);
      const fontSize = 11;
      const margin = 50;
      const lineH = fontSize * 1.5;

      const lines = [];
      text.split('\n').forEach(para => {
        if (!para.trim()) { lines.push(''); return; }
        const words = para.split(' ');
        let cur = '';
        words.forEach(w => {
          const test = cur ? cur + ' ' + w : w;
          const width = font.widthOfTextAtSize(test, fontSize);
          if (width > 595 - margin * 2) { lines.push(cur); cur = w; }
          else cur = test;
        });
        if (cur) lines.push(cur);
      });

      let page = pdfDoc.addPage([595, 842]);
      let y = 842 - margin;
      lines.forEach(line => {
        if (y < margin + lineH) { page = pdfDoc.addPage([595, 842]); y = 842 - margin; }
        if (line) page.drawText(line, { x: margin, y, size: fontSize, font, color: rgb(0, 0, 0) });
        y -= lineH;
      });

      showProgress('word-to-pdf', 95);
      const bytes = await pdfDoc.save();
      showProgress('word-to-pdf', 100);
      hideProgress('word-to-pdf');
      download(bytes, files[0].name.replace('.docx', '') + '.pdf');
      showStatus('word-to-pdf', '✅ Word document converted to PDF!', 'success');
    } catch(e) {
      hideProgress('word-to-pdf');
      showStatus('word-to-pdf', '❌ Error: ' + e.message, 'error');
    }
  }

  // ===================== ROTATE =====================
  async function runRotate() {
    const files = fileStore['rotate'];
    if (!files || files.length === 0) { showStatus('rotate', '⚠️ Please select a PDF file.', 'error'); return; }
    try {
      showProgress('rotate', 20);
      const { PDFDocument, degrees } = PDFLib;
      const ab = await files[0].arrayBuffer();
      const doc = await PDFDocument.load(ab);
      doc.getPages().forEach(p => p.setRotation(degrees(rotationAngle)));
      showProgress('rotate', 80);
      const bytes = await doc.save();
      showProgress('rotate', 100);
      hideProgress('rotate');
      download(bytes, 'rotated.pdf');
      showStatus('rotate', `✅ PDF rotated ${rotationAngle}° successfully!`, 'success');
    } catch(e) {
      hideProgress('rotate');
      showStatus('rotate', '❌ Error: ' + e.message, 'error');
    }
  }

  // ===================== WATERMARK =====================
  async function runWatermark() {
    const files = fileStore['watermark'];
    const wText = document.getElementById('watermark-text').value.trim() || 'CONFIDENTIAL';
    if (!files || files.length === 0) { showStatus('watermark', '⚠️ Please select a PDF file.', 'error'); return; }
    try {
      showProgress('watermark', 20);
      const { PDFDocument, StandardFonts, rgb, degrees } = PDFLib;
      const ab = await files[0].arrayBuffer();
      const doc = await PDFDocument.load(ab);
      const font = await doc.embedFont(StandardFonts.HelveticaBold);
      const pages = doc.getPages();
      showProgress('watermark', 50);
      pages.forEach(page => {
        const { width, height } = page.getSize();
        const fontSize = Math.min(width, height) * 0.1;
        const textWidth = font.widthOfTextAtSize(wText, fontSize);
        page.drawText(wText, {
          x: (width - textWidth) / 2,
          y: (height - fontSize) / 2,
          size: fontSize,
          font,
          color: rgb(0.8, 0.1, 0.1),
          opacity: 0.25,
          rotate: degrees(45),
        });
      });
      showProgress('watermark', 90);
      const bytes = await doc.save();
      showProgress('watermark', 100);
      hideProgress('watermark');
      download(bytes, 'watermarked.pdf');
      showStatus('watermark', '✅ Watermark added successfully!', 'success');
    } catch(e) {
      hideProgress('watermark');
      showStatus('watermark', '❌ Error: ' + e.message, 'error');
    }
  }
</script>
</body>
</html>
